###Homework on "Modules and files".

used:
```bash
    node index hide path data.json
    node index seek path
```
example:
```bash
    node index hide ./field ./pokemons.json
    node index seek ./field
```
